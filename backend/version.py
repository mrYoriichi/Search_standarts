"""Версия и вариант сборки клиентского приложения.

APP_VERSION шлётся в `X-App-Version` при запросах к серверу лицензий. Сервер
отвечает 426 Upgrade Required, если эта версия младше MIN_SUPPORTED_VERSION.

При выпуске новой сборки — обновить здесь (версию продублировать в
installer.iss), проверить PUBLIC_BUILD, потом собрать установщик. См. BUILD.md.
"""

APP_VERSION = "0.4.0"

# Один код — две сборки (решение 2026-07-15, НЕ форк).
# True  — публичная сборка: проверка лицензии fail-open. Недоступный сервер
#         лицензий НИКОГДА не блокирует работу (см. compute_effective_status);
#         блокируют только явные ответы сервера — отзыв и 426.
# False — пилотная сборка: офлайн дольше грейс-периода (1 день) блокирует UI.
PUBLIC_BUILD = True
