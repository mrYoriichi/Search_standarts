"""Версия клиентского приложения.

Шлётся в `X-App-Version` при запросах к серверу лицензий. Сервер
отвечает 426 Upgrade Required, если эта версия младше MIN_SUPPORTED_VERSION.

При выпуске новой сборки — обновить здесь, потом собрать установщик.
"""

APP_VERSION = "0.2.0"
