# Прямые зависимости проекта Search_standarts.
# Развёртывание окружения с нуля:
#   python -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt

# Пайплайн обработки PDF
docling==2.93.0          # парсинг PDF в структурированный JSON

# LLM и токены
openai==2.37.0           # OpenAI API (vision + embeddings + generate_answer)
tiktoken==0.13.0         # подсчёт токенов для лимитов и стоимости
python-dotenv==1.2.2     # загрузка OPENAI_API_KEY из .env

# Web backend
fastapi==0.136.1         # web-фреймворк (API)
uvicorn[standard]==0.47.0  # ASGI-сервер для запуска FastAPI
httpx==0.28.1            # HTTP-клиент для запросов на сервер лицензий (F4+)

# База данных
sqlalchemy==2.0.49       # ORM: Python-классы ↔ таблицы БД (SQLite сейчас, Postgres потом)
python-multipart==0.0.29

# Поиск
rank_bm25==0.2.2         # BM25-индекс для лексического поиска по чанкам
