# Dev-инструменты (НЕ попадают в дистрибутив). Версии приколоты, чтобы CI
# совпадал с локальным окружением: неприколотый ruff 0.16.0 молча включил
# новые правила и уронил CI 96 «ошибками» в нетронутых файлах (2026-07-29).
# Обновлять — осознанным отдельным коммитом вместе с фиксом новых находок.
ruff==0.15.21
pytest==9.1.1
